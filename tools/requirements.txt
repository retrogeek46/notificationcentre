# Media session access (winsdk works better with async)
winsdk

# Image processing
Pillow      # Album art resizing

# HTTP requests
requests

# System monitoring
psutil
pynvml      # NVIDIA GPU stats
wmi         # CPU temperature (Windows)
pywin32     # Window detection
