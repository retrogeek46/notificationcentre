meta {
  name: Change Screen to Notifs
  type: http
  seq: 5
}

post {
  url: http://{{notif_url}}/screen?name=notif
  body: none
  auth: inherit
}

params:query {
  name: notif
}

settings {
  encodeUrl: true
  timeout: 0
}
